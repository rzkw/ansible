{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "ansible": {
      "type": "local",
      "command": ["npx", "-y", "@ansible/ansible-mcp-server", "--stdio"],
      "enabled": true
    }
  }
}
